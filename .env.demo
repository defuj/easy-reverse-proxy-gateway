# ========================================
# DEMO CONFIGURATION
# ========================================
# This is a demo configuration for testing
# Copy this to .env to test with localhost services

# Server Configuration
SERVER_NAME=localhost
NGINX_PORT=80
WORKER_PROCESSES=auto
WORKER_CONNECTIONS=1024

# ========================================
# DEMO SERVICES (localhost)
# ========================================

# Frontend App (e.g., React/Vue/Nuxt on port 3000)
FRONTEND_HOST=localhost:3000
FRONTEND_ROUTES=/,/about,/contact
FRONTEND_PRIORITY=1

# Backend API (e.g., Express/FastAPI on port 8080)
API_HOST=localhost:8080
API_ROUTES=/api
API_PRIORITY=2

# Admin Panel (e.g., Admin dashboard on port 3001)
ADMIN_HOST=localhost:3001
ADMIN_ROUTES=/admin
ADMIN_PRIORITY=3

# ========================================
# NOTES:
# ========================================
# Untuk testing:
# 1. Start semua services di localhost (ports: 3000, 8080, 3001)
# 2. Copy file ini: cp .env.demo .env
# 3. Run: npm run dev
# 4. Test: curl http://localhost/
