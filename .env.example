# ========================================
# NGINX GATEWAY CONFIGURATION
# ========================================

# Server Configuration
SERVER_NAME=localhost
NGINX_PORT=80
WORKER_PROCESSES=auto
WORKER_CONNECTIONS=1024

# Path Rewrite Configuration
# Set to 'true' untuk enable automatic path rewriting dan redirect fixing
# Berguna jika service running di root (/) tapi diakses via subpath (/app1, /app2)
ENABLE_PATH_REWRITE=true

# ========================================
# SERVICE CONFIGURATIONS
# ========================================
# Format untuk setiap service:
# NAMA_APP_HOST=IP:PORT
# NAMA_APP_ROUTES=route1,route2,route3
# NAMA_APP_PRIORITY=number (optional, lower = higher priority)

# Landing App (Nuxt)
LANDING_APP_HOST=192.168.1.100:5004
LANDING_APP_ROUTES=/,/products,/pricing,/terms-conditions,/privacy-policy
LANDING_APP_PRIORITY=1

# Customer App (Nuxt)
CUSTOMER_APP_HOST=192.168.1.101:5002
CUSTOMER_APP_ROUTES=/dashboard,/profile,/orders
CUSTOMER_APP_PRIORITY=2

# Admin App (React)
# ADMIN_APP_HOST=192.168.1.102:3000
# ADMIN_APP_ROUTES=/admin
# ADMIN_APP_PRIORITY=3

# API Service (Express)
# API_APP_HOST=192.168.1.103:8080
# API_APP_ROUTES=/api,/graphql
# ADMIN_APP_PRIORITY=4

# ========================================
# NOTES:
# ========================================
# 1. PRIORITY: Lower number = higher priority (akan di-generate lebih dulu)
# 2. ROUTES: Pisahkan dengan koma, tanpa spasi
# 3. Default app: App tanpa ROUTES atau priority paling rendah akan jadi catch-all
# 4. Setelah edit, jalankan: npm run dev atau npm run generate
