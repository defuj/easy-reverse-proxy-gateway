# Auto-generated nginx configuration
# Generated at: 2025-12-04T08:21:42.510Z
# DO NOT EDIT THIS FILE MANUALLY - Edit .env instead and run: npm run generate

user nginx;
worker_processes auto;

error_log /var/log/nginx/error.log notice;
pid /var/run/nginx.pid;

events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent "$http_referer" '
                    '"$http_user_agent" "$http_x_forwarded_for"';

    access_log /var/log/nginx/access.log main;

    sendfile on;
    keepalive_timeout 65;

    # Global proxy settings
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection 'upgrade';
    proxy_cache_bypass $http_upgrade;

    # LANDING_APP
    upstream landing_app {
        server 192.168.1.100:5004;
    }

    # CUSTOMER_APP
    upstream customer_app {
        server 192.168.1.101:5002;
    }

    server {
        listen 80;
        server_name localhost;

        # LANDING_APP - /
        location = / {
            proxy_pass http://landing_app;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_buffers 16 512k;
            proxy_buffer_size 512k;
            proxy_busy_buffers_size 512k;
            proxy_read_timeout 120s;
            proxy_send_timeout 120s;
        }

        # LANDING_APP - /products
        location = /products {
            proxy_pass http://landing_app;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_buffers 16 512k;
            proxy_buffer_size 512k;
            proxy_busy_buffers_size 512k;
            proxy_read_timeout 120s;
            proxy_send_timeout 120s;
        }

        # LANDING_APP - /pricing
        location = /pricing {
            proxy_pass http://landing_app;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_buffers 16 512k;
            proxy_buffer_size 512k;
            proxy_busy_buffers_size 512k;
            proxy_read_timeout 120s;
            proxy_send_timeout 120s;
        }

        # LANDING_APP - /terms-conditions
        location = /terms-conditions {
            proxy_pass http://landing_app;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_buffers 16 512k;
            proxy_buffer_size 512k;
            proxy_busy_buffers_size 512k;
            proxy_read_timeout 120s;
            proxy_send_timeout 120s;
        }

        # LANDING_APP - /privacy-policy
        location = /privacy-policy {
            proxy_pass http://landing_app;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_buffers 16 512k;
            proxy_buffer_size 512k;
            proxy_busy_buffers_size 512k;
            proxy_read_timeout 120s;
            proxy_send_timeout 120s;
        }

        # CUSTOMER_APP - /dashboard
        location = /dashboard {
            proxy_pass http://customer_app;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_buffers 16 512k;
            proxy_buffer_size 512k;
            proxy_busy_buffers_size 512k;
            proxy_read_timeout 120s;
            proxy_send_timeout 120s;
        }

        # CUSTOMER_APP - /profile
        location = /profile {
            proxy_pass http://customer_app;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_buffers 16 512k;
            proxy_buffer_size 512k;
            proxy_busy_buffers_size 512k;
            proxy_read_timeout 120s;
            proxy_send_timeout 120s;
        }

        # CUSTOMER_APP - /orders
        location = /orders {
            proxy_pass http://customer_app;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_buffers 16 512k;
            proxy_buffer_size 512k;
            proxy_busy_buffers_size 512k;
            proxy_read_timeout 120s;
            proxy_send_timeout 120s;
        }

        # Default catch-all (CUSTOMER_APP)
        location / {
            proxy_pass http://customer_app;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_buffers 16 512k;
            proxy_buffer_size 512k;
            proxy_busy_buffers_size 512k;
            proxy_read_timeout 120s;
            proxy_send_timeout 120s;
        }
    }
}
