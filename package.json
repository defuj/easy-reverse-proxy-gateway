{
  "name": "reverse-proxy-tool",
  "version": "2.1.0",
  "description": "Nginx reverse proxy gateway untuk multiple services dengan auto path rewrite & redirect fix",
  "scripts": {
    "generate": "node generate-config.js",
    "check": "node validate-env.js",
    "dev": "npm run generate && docker-compose up",
    "start": "npm run generate && docker-compose up -d",
    "stop": "docker-compose down",
    "restart": "npm run generate && docker-compose restart",
    "logs": "docker-compose logs -f nginx",
    "reload": "npm run generate && docker-compose exec nginx nginx -s reload",
    "test": "docker-compose exec nginx nginx -t",
    "validate": "node validate-env.js && node generate-config.js",
    "setup": "cp .env.example .env && echo '\n✅ .env file created! Edit .env and then run: npm run dev'"
  },
  "keywords": ["nginx", "reverse-proxy", "gateway"],
  "author": "",
  "license": "ISC"
}
